<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Dev: allow cleartext to emulator host / LAN. Tighten for production HTTPS-only. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
